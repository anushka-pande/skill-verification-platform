PHASE 1 (Days 1–3) — Platform Foundation
Submission History Page
/submissions route
Show:
task name
score
date
status
Store Proper Data in DB

Update submission model:

"user_id"
"task_id"
"final_score"
"created_at"
"language"

Don’t skip language (needed for multi-language)

Profile Page Upgrade

Add:

Total submissions
Average score
Best score
Tasks attempted
PHASE 1.5 (Days 4–5) — 🔥 MULTI-LANGUAGE EXECUTION
Backend Executor Upgrade

Support:

Python (already)
C++ (add now)

Structure:

execute_code(code, input, language)
C++ Execution Flow
Save .cpp
Compile → g++ file.cpp -o out
Run → ./out

Handle:

compile errors
runtime errors
Update Submission API

Add:

"language": "python" / "cpp"
UI Language Selector (Editor Page)

Add dropdown:

Python | C++

Pass it to backend

After this phase, you can confidently say:

“System supports multi-language execution”

PHASE 2 (Days 6–8) — Smart Platform Features
Performance Analytics

Backend:

avg score
attempts
success rate

Frontend:

Average Score: 78
Success Rate: 65%
Difficulty-Based UI
Color badges:
Easy → green
Medium → yellow
Hard → red
Filter by difficulty
Better Evaluation Display

Instead of just score:

Show:

passed test cases
failed test cases
error messages

 PHASE 3 (Days 9–11) — Intelligence Layer
 Skill Level Classification (ML-lite)
>80 → Advanced
50–80 → Intermediate
<50 → Beginner

Store in DB

Show in profile

Basic Plagiarism Indicator (Simple Rule-Based)

Options:

compare code length
simple string similarity

Return:

Low / Medium / High similarity

Keep it simple, don’t overthink

PHASE 4 (Days 12–15) — Product Polish
 UI Polish
spacing consistency
hover effects
smoother layout
better alignment
 Landing Page
Hero section
“Start Coding” button